import os

from config import TARGET_CLASSES, GT_MAPPING, MASK_FILE_NAMES, COCO_JSON_PATH,PIPELINES, PRED_MASK_PATH, CLIPPREDICTION_CSV, METADATA_DIRECTORY, OUTPUT_DIR


class EvaluatePipeline:
    def __init__(self):
        self.TARGET_CLASS = TARGET_CLASSES
        self.GT_MAPPING = GT_MAPPING
        self.MASK_FILE_NAMES = MASK_FILE_NAMES
        self.COCO_JSON_PATH = COCO_JSON_PATH
        self.PIPELINES = PIPELINES
        self.PRED_MASK_PATH = PRED_MASK_PATH
        self.CLIPPREDICTION_CSV = CLIPPREDICTION_CSV
        self.METADATA_DIRECTORY = METADATA_DIRECTORY
        self.OUTPUT_DIR = OUTPUT_DIR

